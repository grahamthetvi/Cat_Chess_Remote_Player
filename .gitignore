# Per-machine arcade launch settings
scripts/arcade.config.ps1

# Host secrets and runtime state (never commit)
data/arcade.secrets.json
data/known-ips.json
data/session-note.json
data/audit.log
data/portal-service.out.log
data/portal-service.err.log
data/*.json
!data/*.example.json

# Local NSSM source clone (use prebuilt binary from nssm.cc / winget for the service)
/nssm/

# Source clones — install release binaries instead (winget Sunshine, C:\Tools\moonlight-web)
/Sunshine/
/moonlight-web-stream/
